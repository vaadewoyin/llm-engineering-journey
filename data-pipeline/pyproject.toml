[project]
name = "data-pipeline"
version = "0.1.0"
description = "data pipeline for new dataset for week 7 onwards"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "comet-ml>=3.58.5",
    "docling>=2.121.0",
    "semanticscholar>=0.12.0",
]
